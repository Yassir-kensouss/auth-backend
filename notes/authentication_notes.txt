step 1 : fill the user module inside module file with required attributes then crypt the password then export the module

step 2 : add users file inside routes folder to add signup routes that will trigger the signup method imported from the userController (step 3)

step 3 : add the userController file inside controllers folder and import the module then add new method for signing up and make a new instance from the usr module

step 4 : validate using express validator

step 5 (signin) : find the user by verifying the entries , then generate token using jwt and send a cookie using cookie-parser , then send the user info as a json 

step 6 (signout) : create new controller and kill the cookie by clearCookie('cookie name)and return a message

